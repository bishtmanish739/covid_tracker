class Global {
  final int newConfirm;
  final int totalConfirm;
  final int totalDeath;
  final int newDeath;
  final int newRecovered;
  final int totalRecovered;

  Global(this.newConfirm, this.totalConfirm, this.totalDeath, this.newDeath,
      this.newRecovered, this.totalRecovered);
}
